# 專案紀錄：外觀系統（appearance）

## 已完成

### 1. `src/common/php/user_appear.php`
- GET：依 `Authorization: Bearer token` 查登入者自己的外觀，回傳 `{ success, data: { gender, hair, eyes, skin } }`
- POST：讀取 `php://input` 的 JSON body（`gender`/`selectedHair`/`selectedEyes`/`selectedSkin`），gender 轉換為 `g01`/`g02`，`DELETE` 舊資料後 `INSERT` 新的4筆，包在 `beginTransaction()`/`commit()` 裡
- 已修正過的 bug：`$_SEVER`→`$_SERVER` 拼字錯、`$memberId` 在驗證 `$member` 存在前就取值的順序問題、DELETE 執行到錯的 statement（`$stmt`而非`$stmt3`）、INSERT 少帶 `$memberId` 參數、POST 分支結尾漏回傳 JSON

### 2. `src/common/php/user_appear_public.php`（新檔案）
- 不驗證 token，改用 `?user_id=` query string 查任何人的外觀（大頭貼是公開資訊）
- 內部查詢/組資料邏輯跟 `user_appear.php` 的 GET 分支相同，已確認寫法正確

### 3. `src/layouts/book-room/BookroomPanelAppearanceArea.vue`
- `created()` 生命週期：打 GET 同步登入者當前外觀，回填 `gender`/`selectedHair`/`selectedEyes`/`selectedSkin`
- `confirmChangeAppearance()`：確認後打 POST 存檔
- 修正 bug：`selectGender(g)` 新增，切換性別按鈕改呼叫這個 method——原本只改 `gender`、沒同步改三個外觀選項的 appear_id 前綴（女 `fe/fh/fs`、男 `me/mh/ms`），導致切換性別後對應圖片對不到（例如「沒有眼睛」）

## 進行中：大頭貼同步顯示（會員專區、討論區共用）

**目標**：GET 到的外觀資料，用 `<canvas>` 疊圖（膚色→瞳色→髮色）畫成一張大頭貼，讓多個地方（會員專區、討論區大頭照等）都能重複使用同一套邏輯，且各自可以指定不同的畫布尺寸。

**確定的架構**：Composition API，一個 component + 一個 composable 分工：
- `src/components/front/PhotoSticker.vue`（component）：只放 `<canvas ref="ref01">` 樣板 + 呼叫 composable，`props` 接 `userId`、`width`、`height`
- `src/composables/useDrawAvatar.js`（composable）：邏輯層——依 `userId` 打 `user_appear_public.php`、把三層圖片依 `appearImageMap` 對照表 `drawImage` 疊上 canvas

**PhotoSticker.vue 目前進度**：
```vue
<template>
  <canvas ref="avatarCanvas" width="64" height="64"></canvas>
</template>

<script>
import femaleSkinLight/Medium/Dark, femaleEyesBlack/Blue/Green, femaleHairBlack/Blue/Brown,
       maleSkinLight/Medium/Dark, maleEyesBlack/Blue/Green, maleHairBlack/Blue/Brown
  // 全部 import 完成，來源 src/assets/images/appear/character-for-register/

const appearImageMap = {
  fs1/fs2/fs3, fe1/fe2/fe3, fh1/fh2/fh3,
  ms1/ms2/ms3, me1/me2/me3, mh1/mh2/mh3,
}; // 已建立完成，對照正確

export default {
  props: {
    userId: { type: [String, Number], required: true },
  },
};
</script>
```
- 還沒做：改成 `<script setup>`（Composition API 寫法）、canvas 的 `width`/`height` 改成綁定 `props` 傳進來的值（不能寫死 64）、改成呼叫 `useDrawAvatar` 而不是自己內部處理

**useDrawAvatar.js 目前進度（草稿，有已知問題待修）**：
```js
function loadImage(src) {
  return new Promise((resolve) => {
    const img = new Image();
    img.onload = () => resolve(img);
    img.src = src;
  });
}

async function drawAvatar(canvas, appearance) {
  const ctx = canvas.getContext("2d");
  const skinImg = await loadImage(appearImageMap[appearance.skin]);
  const eyesImg = await loadImage(appearImageMap[appearance.eyes]);
  const hairImg = await loadImage(appearImageMap[appearance.hair]);
  ctx.drawImage(skinImg, 0, 0, 64, 64);
  ctx.drawImage(eyesImg, 0, 0, 64, 64);
  ctx.drawImage(hairImg, 0, 0, 64, 64);
}

export function useDrawAvatar(canvas, appearance) {
  watch(canvas, () => {
    drawAvatar(canvas, appearance);
  });
  return { a };
}
```

**已知待修問題（下次繼續處理）**：
1. 缺 `import { ref, watch } from "vue";`
2. `appearImageMap` 這個對照表要搬進這支檔案（或獨立成共用檔案 import），目前只存在 `PhotoSticker.vue` 裡
3. 函式簽名要改成 `useDrawAvatar(canvasRef, userId, width, height)`——composable 要**自己內部 fetch** `user_appear_public.php?user_id=`，不是外面把 `appearance` 準備好傳進來
4. `canvas.getContext(...)` 要改成 `canvas.value.getContext(...)`（canvas 是 Vue ref，要用 `.value` 才能拿到真正的 DOM）
5. `ctx.drawImage(..., 0, 0, 64, 64)` 三處寫死 `64`，要改成用傳進來的 `width`/`height` 參數，否則不同元件傳不同尺寸也沒用
6. `return { a };` 的 `a` 未定義，要刪掉或改成有意義的回傳值（目前設計上 composable 不一定需要回傳東西）
7. 觸發畫圖的時機：原本用 `watch(canvas, ...)`，需要確認「canvas ref 掛載完成」和「API 資料抓回來」兩個條件都到齊才畫圖，可能要搭配 `onMounted` 一起設計

**其他確認過的設計決定**：
- `<canvas>` 的 HTML `width`/`height` 屬性要直接綁定實際像素數字（`:width="width"`），不要用 CSS `%`，避免畫布解析度跟顯示尺寸不一致造成模糊/跑版；響應式排版交給外層包住 canvas 的 `div` 處理
- 同一個 canvas 內三層圖片疊放不會因為傳入不同 `width`/`height` 而彼此對不齊，因為三層永遠畫到同一個目標矩形、等比例縮放——已確認安全，不需額外處理

## 下次接續點
下次回來直接從「已知待修問題」7點開始修 `useDrawAvatar.js`，然後回頭把 `PhotoSticker.vue` 改成呼叫這個 composable 的版本。
