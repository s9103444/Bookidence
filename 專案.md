## 假資料的存放不用pinia的方式去保留
   - 假資料基本都是針對該頁面選染所需與開發所需，把假資料寫成json檔案，用fetch的方式去拿假資料。

## 善用環境變數 .env
   - 實際運行環境與開發環境不會在同一主機上，此時自動化調整指向資料庫可以使開發過程更便捷。

## 統整頁面、元件、組件 ( 組件、元件定義沒有固定 )
   - 頁面 => router 實際走到的第一層頁面 ( /study 、 /guilds )
   - 元件 => 不屬於任何一個頁面，時常出現在任一頁面，如: 圖卡、button
   - 組件 => 頁面裡的secction 
   - 上述三者關係確立並整理好後，可大幅保持專案整潔

